{% from "./challengeCard.njk" import challengeCard %}

{% macro challengeList(challenges) %}
<section class="challenge-list">
  {% for challenge in challenges %}
    {{ challengeCard(challenge.data.title, challenge.slug, challenge.data.summary, index=loop.index) }}
  {% endfor %}
</section>
{% endmacro %}
